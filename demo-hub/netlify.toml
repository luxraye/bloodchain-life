# Netlify: base directory = demo-hub (set in site settings)
[build]
  command = "cd .. && yarn install && yarn workspace demo-hub build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
