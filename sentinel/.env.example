# Optional: connect to a running Instances (g-instances) API
VITE_INSTANCES_API_URL=http://localhost:3000
VITE_INSTANCES_API_KEY=your-tenant-api-key

# Full Instances admin UI (Next.js) — linked from Sentinel settings
VITE_INSTANCES_ADMIN_URL=http://localhost:3000
